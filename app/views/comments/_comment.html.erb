<li id="comment_<%=comment.id %>">
  <%= comment.body %>,
  <%= time_ago_in_words(comment.created_at) %> ago
  <%if @memo.user == current_user%>
  <%= link_to fa_icon("times-circle-o"), [comment.memo, comment], method: :delete, remote: true, data: { confirm: "정말 삭제하시겠어요?!!!" } %>
<%end%>
</li>

